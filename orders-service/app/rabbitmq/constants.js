const exchangeName = 'exchange';
const routingKey = 'user.created';
const queueName = 'user-created';
const routingKey2 = 'send.email';
const routingKey3 = 'botUser.created';

module.exports = {
  exchangeName,
  routingKey,
  queueName,
  routingKey2,
  routingKey3,
};
